# Ralph Progress Log

## Codebase Patterns

- UI routes receive `getPublicState()` snapshots; any mutation that must persist or affect derived room membership should go through `src/state.ts` helpers, especially `updateAgentState()` and `rebuildRooms()`.
- The current `node --test test/*.test.mjs` harness imports built files from `dist/`, so run `npm run build` before `npm test` whenever source changes affect test coverage.
- The AWN SDK calls the same `onLeave(agentId)` hook for explicit `world.leave` and idle eviction; distinguish the two inside `src/hooks.ts` by comparing the agent's `lastSeenAt` to `manifest.lifecycle.idleTimeoutMs`.
- Presence SSE names should mirror the inferred leave path in `src/hooks.ts`: idle eviction broadcasts `agent_offline`, while voluntary exits broadcast `agent_leave`.
- Hook actions should treat `MemoStore` persistence as best-effort after canonical in-memory updates so transient filesystem failures do not break runtime action success.

---

## 2026-03-22 16:34:46 UTC - T-001
- Documented the current canonical runtime mutation path in `src/state.ts`, the compat `/set_state` route divergence in `src/ui-routes.ts`, and the room rebuild dependency point in `src/index.ts`.
- Files changed: `.hal/auto-prd.json`, `.hal/progress.txt`, `src/index.ts`, `src/state.ts`, `src/ui-routes.ts`
- **Learnings for future iterations:**
  - Patterns discovered: UI routes are wired with `getState: () => getPublicState(state)`, so direct mutation inside route handlers does not use the same path as AWN hooks.
  - Gotchas encountered: `POST /set_state` currently mutates the public snapshot instead of calling `updateAgentState()`, which is why room rebuild behavior can diverge.
  - Useful context: Derived `rooms` data is recalculated by `rebuildRooms()` from `state.agents`; startup currently only hydrates memo data, not agent state.
---

## 2026-03-22 16:38:29 UTC - T-002
- Routed compat `POST /set_state` through a new `setAgentState` dependency so UI route updates use `updateAgentState()` instead of mutating the public snapshot returned by `getState()`.
- Added a focused `node:test` regression that exercises `/set_state` through Fastify and verifies the backing state's `rooms` buckets rebuild from the updated persisted agent area.
- Files changed: `.hal/auto-prd.json`, `.hal/progress.txt`, `src/index.ts`, `src/ui-routes.ts`, `test/ui-routes.test.mjs`
- **Learnings for future iterations:**
  - Patterns discovered: UI routes can safely mutate canonical state by receiving explicit state helper callbacks from `src/index.ts` rather than direct access to the mutable backing object.
  - Gotchas encountered: The current test command executes `.mjs` files against `dist/`, so building and testing in parallel can produce false failures against stale output.
  - Useful context: `Fastify.inject()` is enough to cover compat route regressions without starting a long-running server, and the route only needs an existing agent to target the first office occupant.
---

## 2026-03-22 16:41:36 UTC - T-003
- Split lifecycle leave handling so stale agents are marked offline in place while recent agents are still removed on voluntary leave.
- Added hook-level regressions covering idle eviction state preservation and voluntary leave removal.
- Files changed: `.hal/auto-prd.json`, `.hal/progress.txt`, `src/hooks.ts`, `test/hooks.test.mjs`
- **Learnings for future iterations:**
  - Patterns discovered: The SDK does not pass an eviction reason into `onLeave()`, so lifecycle behavior has to infer idle eviction from the tracked agent timestamp and timeout.
  - Gotchas encountered: `markOffline()` keeps the agent in `state.agents` but removes them from derived room occupancy because `rebuildRooms()` only includes `online` agents.
  - Useful context: Hook regressions can exercise `createOfficeHooks()` directly with a minimal `broadcast()` spy and a temporary `MemoStore`, without booting Fastify.
---

## 2026-03-22 16:44:02 UTC - T-004
- Aligned lifecycle presence broadcasts so inferred idle eviction emits `agent_offline` and voluntary leave continues to emit `agent_leave`.
- Extended the hook regression coverage to assert the exact SSE event names emitted for both leave paths.
- Files changed: `.hal/auto-prd.json`, `.hal/progress.txt`, `src/hooks.ts`, `test/hooks.test.mjs`
- **Learnings for future iterations:**
  - Patterns discovered: `onLeave()` owns both the canonical presence mutation and the SSE event name, so tests should assert state changes and emitted event names together.
  - Gotchas encountered: Because the test harness imports `dist/`, event-regression assertions can appear stale unless the build is refreshed before running `npm test`.
  - Useful context: A simple `broadcasts` array spy in `test/hooks.test.mjs` is enough to verify lifecycle SSE behavior without spinning up the SSE manager or Fastify.
---

## 2026-03-22 16:45:49 UTC - T-005
- Wrapped `post_memo` persistence in a best-effort `try/catch` so memo entries still land in `state.todayMemos`, emit the memo SSE event, and return `{ ok: true }` when disk writes fail.
- Added a focused hook regression that injects a throwing `memoStore.append()` implementation and asserts in-memory memo retention plus a successful action result.
- Files changed: `.hal/auto-prd.json`, `.hal/progress.txt`, `src/hooks.ts`, `test/hooks.test.mjs`
- **Learnings for future iterations:**
  - Patterns discovered: `post_memo` mutates canonical in-memory memo state before persistence, so persistence failures should be logged and tolerated instead of aborting the action.
  - Gotchas encountered: Hook tests can inject a minimal `memoStore` double directly into `createOfficeHooks()`; it does not need a real filesystem-backed `MemoStore` instance for failure-path coverage.
  - Useful context: The memo SSE broadcast should continue to use the same in-memory entry object even when persistence is skipped, so action success and client updates stay aligned.
---
